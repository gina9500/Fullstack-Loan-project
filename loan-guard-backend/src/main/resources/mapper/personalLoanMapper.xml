<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loanguard.backend.mapper.PersonalLoanMapper">
    
    <insert id="insertPersonalLoan" parameterType="com.loanguard.backend.model.PersonalLoan">
        INSERT INTO personal_loan_application (
            user_id, name, id_number, birth_date, id_card_expiry_date,
            mobile_no, email, appointment_no, status, create_time, update_time
        ) VALUES (
            #{userId}, #{name}, #{idNumber}, #{birthDate}, #{idCardExpiryDate},
            #{mobileNo}, #{email}, #{appointmentNo}, #{status}, NOW(), NOW()
        );
    </insert>
    
    <select id="selectByAppointmentNo" resultType="com.loanguard.backend.model.PersonalLoan">
        SELECT * FROM personal_loan_application WHERE appointment_no = #{appointmentNo};
    </select>
    
    <select id="selectByUserId" resultType="com.loanguard.backend.model.PersonalLoan">
        SELECT * FROM personal_loan_application WHERE user_id = #{userId};
    </select>
    
    <update id="updatePersonalLoanByUserId" parameterType="com.loanguard.backend.model.PersonalLoan">
        UPDATE personal_loan_application 
        SET name = #{name}, id_number = #{idNumber}, birth_date = #{birthDate}, 
            id_card_expiry_date = #{idCardExpiryDate}, mobile_no = #{mobileNo}, 
            email = #{email}, status = #{status}, update_time = NOW()
        WHERE user_id = #{userId};
    </update>
    
    <select id="countByAppointmentNo" resultType="int">
        SELECT COUNT(*) FROM personal_loan_application WHERE appointment_no = #{appointmentNo};
    </select>
</mapper>